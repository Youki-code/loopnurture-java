server.port: 8888
spring:
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          searchLocations: classpath:/config
        git:
          uri: https://github.com/Youki-code/loopnurture-java
          default-label: main

